#version 150
in vec3 Position;in vec2 UV0;uniform sampler2D Sampler0;uniform mat4 ModelViewMat;uniform mat4 ProjMat;out vec2 texCoord0;vec4 getVertexColor(sampler2D Sampler,int vertexID,vec2 coords){ivec2 texSize=textureSize(Sampler,0);vec2 offset=vec2(0.);float pixelX=1./texSize.x/2.;float pixelY=1./texSize.y/2.;vertexID=vertexID%4;switch(vertexID){case 0:offset=vec2(-pixelX,pixelY);break;case 1:offset=vec2(pixelX,pixelY);break;case 2:offset=vec2(pixelX,-pixelY);break;case 3:offset=vec2(-pixelX,-pixelY);break;default:offset=vec2(0.);break;}return texture(Sampler,coords-offset);}void main(){gl_Position=ProjMat*ModelViewMat*vec4(Position,1.);texCoord0=UV0;vec4 color=getVertexColor(Sampler0,gl_VertexID,texCoord0);if(color.r==38./255.&&color.g==26./255.&&color.b==28./255.)gl_Position=ProjMat*ModelViewMat*vec4(Position+vec3(-5.,0.,0.),1.);if(color.r==37./255.&&color.g==40./255.&&color.b==30./255.)gl_Position=ProjMat*ModelViewMat*vec4(Position+vec3(-5.,0.,0.),1.);}